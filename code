<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Italianno&family=Noto+Sans+Nabataean&family=Noto+Sans+SC:wght@600;900&family=Roboto:ital,wght@0,100;0,400;0,700;1,500&family=Sofia+Sans+Condensed:ital,wght@0,100;0,700;1,400&display=swap" rel="stylesheet">



font-family: 'Italianno', cursive;
font-family: 'Noto Sans Nabataean', sans-serif;
font-family: 'Noto Sans SC', sans-serif;
font-family: 'Roboto', sans-serif;
font-family: 'Sofia Sans Condensed', sans-serif;

font-family: 'Roboto', sans-serif;













.btn{
    &:link,
    &:visited{
    text-transform: uppercase;
    text-decoration: none;
    padding: 1.0rem 2rem;
    display: inline-block;
    border-radius: 10rem;
    transition: all .2s;
    position: relative;
    font-size: 1.4rem;
    transition: box-shadow .2s;
}

    &:hover{
        transform: translateY(-3px);
        box-shadow: 0 1rem 2rem rgba($color-black,.2);

        &::after{
            transform: scaleX(1.4) scaleY(1.6);
            opacity: 0;
        }
    }

    &:active{
        transform: translateY(-1px);
        box-shadow: 0 .5rem 1rem rgba($color-black,.2);
    }

    
  &--white{
        background-color: $color-white;
        color:$color-grey-dark;

        &::after{
            background-color: $color-white;
        }
    }

    &--green{
        background-color: $color-primary;
        color:$color-white;

        &::after{
            background-color: $color-primary;
        }
    }    
  &::after{
        content: "";
        display: inline-block;
        height: 100%;
        width: 100%;
        border-radius: 10rem;
        position: absolute;
        top: 0;
        left: 0;
        z-index: -1;
        transition: all .4s;
    }
 
  
  &--animated{
        animation: moveInBottom .5s ease-out .75s;
        animation-fill-mode: backwards;
    }

}
  
.btn-text{
    &:link,
    &:visited{
        color: $color-primary;
        display: inline-block;
        text-decoration: none;
        border-bottom:1px solid $color-primary !important;
        padding: 9px;
        font-size: $default-font-size ;
        transition: all .2s;
        border-radius: 0;
        transition: box-shadow .2s;
    }

    &:hover{
        background-color:$color-primary ;
        color: $color-white;
        box-shadow: 0 1rem 2rem rgba($color-black, .3);
        transform: translateY(-.2rem);
    }
 
    &:active{
        box-shadow: 0 .5rem 1rem rgba($color-black, .15);
        transform: translateY(0);
    }
}

















sass/components/_bg-video.scss sass/components/_button.scss sass/components/_card.scss sass/components/_composition.scss sass/components/_feature-box.scss sass/components/_form.scss sass/components/_popup.scss sass/components/_story.scss










































